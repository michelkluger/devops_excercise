services:
  meilisearch:
    image: getmeili/meilisearch:v1.13
    ports:
      - "7701:7700"
    environment:
      MEILI_ENV: development
      MEILI_NO_ANALYTICS: "true"
    volumes:
      - meili_data:/meili_data

volumes:
  meili_data:
